import 'package:flutter/material.dart';

class CategoryContact extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Contact of peoples'),
      ),
    );
  }
}
